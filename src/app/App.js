import React from 'react';

import Header from 'app/components/Header';

const App = () => {
  const renderApp = () => <Header />;

  return renderApp();
};

App.propTypes = {};

App.defaultProps = {};

export default App;
